name: Build website

on:
  push:
    branches:
      - main
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v3

    - name: Install Nix
      uses: DeterminateSystems/nix-installer-action@main
      with:
        logger: pretty
        log-directives: nix_installer=trace
        backtrace: full

    - name: Nix cache
      uses: DeterminateSystems/magic-nix-cache-action@main

    - name: Setup Debug Session
      uses: csexton/debugger-action@master

    - name: Build development environment and run code
      run: |
        nix-shell
        echo "Printing paths"
        nix-shell --run 'echo $PATH'
        nix-shell --run 'python -c "import site; print(site.getsitepackages())"'
        nix-shell --run 'which python'
        echo "Printing internal polars and path"
        nix-shell --run 'python -c "import polars; print(polars)"'
        nix-shell --run 'python -c "import sys; print( sys.path )"'
        nix-shell --run 'which jupytext'
        echo "Tests finished"
        nix-shell --run 'jupytext --sync pills/scripts/*.py'
        nix-shell --run 'echo $(which jupytext)'
        nix-shell --run 'quarto check'
        nix-shell --run 'tmp=$(which jupytext) && export PATH="${tmp%/bin/*}/lib/python3.11/site-packages:$PATH" && quarto render'
        # nix-shell --run 'export PATH="$(dirname $tmp):$PATH" && quarto render'
        git config --global user.email "${{ inputs.GITHUB_EMAIL }}"
        git config --global user.name "${{ inputs.GITHUB_USERNAME }}"
        nix-shell --run 'poetry quarto publish gh-pages'

    # - uses: quarto-dev/quarto-actions/setup@v2
    #   with:
    #     version: 1.4.515

    # - name: Publish to GitHub Pages (and render)
    #   uses: b-r
